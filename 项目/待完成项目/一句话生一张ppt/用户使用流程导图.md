# 一句话生一张PPT - 用户使用流程导图

```mermaid
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#E2E8F0', 'edgeLabelBackground':'#ffffff', 'tertiaryColor': '#F1F5F9'}}}%%
flowchart TD
    Start([应用启动]) --> Home[主界面: 输入工作台区]
    
    
    subgraph Stage1["阶段 1：灵感与输入 (Input Stage)"]
        Home --> InputPrompt[/"输入框: '一句话描述PPT内容'"/]
        InputPrompt --> SelectStyle[选择 PPT 视觉风格]
        SelectStyle -->|预设风格| ClickGenerate(("点击 '一键生成'"))
    end
    
    ClickGenerate --> GeneratorEngine
    
    subgraph Stage2["阶段 2：AI 生成调度层 (Generation Engine)"]
        GeneratorEngine{AI 生成引擎调度}
        GeneratorEngine --> |1. 发送提示词| LLM[大语言模型 API]
        LLM -.-> |返回 JSON| ExtractedData[("解析出: 标题、要点、SD提示词")]
        
        ExtractedData --> |2. 发送 SD 提示词| SD_API[图生图 API 队列]
        SD_API -.-> |轮询获取| BG_Image["生成高质量背景图 URL"]
        
        ExtractedData --> Assemble[合成图文数据]
        BG_Image --> Assemble
    end
    
    Assemble --> Preview
    
    subgraph Stage3["阶段 3：预览、编辑与调整 (Preview & Edit Stage)"]
        Preview[工作台展示区: 16:9 幻灯片实时预览]
        Preview --> EditAction{用户满意吗?}
        
        EditAction -->|不满意文案| EditContent[直接在画面上双击修改文字]
        EditAction -->|不满意图片| ReGenImage["点击 '重新生成背景图'"]
        EditAction -->|不满意排版| ChangeLayout[左文右图 / 居中覆盖等一键切换]
        
        EditContent --> Preview
        ReGenImage -->|再次调用 SD API| Assemble
        ChangeLayout --> Preview
    end
    
    EditAction -->|满意| SaveAndExport
    
    subgraph Stage4["阶段 4：分享与留存 (Share & Save Stage)"]
        SaveAndExport{导出选项}
        SaveAndExport -->|生成分享二维码| QRCode["弹出二维码供用户扫码下载/查看"]
        SaveAndExport -->|只存草稿| SyncCloud
    end
    
    QRCode --> SyncCloud[后台静默同步数据至 BaaS]
    SyncCloud --> End([结束])
    
    %% 历史记录侧边工作流
    Home -.-> |点击| HistoryPanel[侧边栏: 历史生成记录]
    HistoryPanel -.-> |拉取 BaaS 数据| ListItems[最近生成的单曲 PPT 列表]
    ListItems -.-> |点击某条记录| Preview
    
    %% 样式定义
    classDef startEnd fill:#10B981,stroke:#047857,stroke-width:2px,color:#fff;
    classDef process fill:#EFF6FF,stroke:#3B82F6,stroke-width:2px;
    classDef io fill:#FEF3C7,stroke:#D97706,stroke-width:2px;
    classDef ai fill:#F5F3FF,stroke:#8B5CF6,stroke-width:2px,stroke-dasharray: 5 5;
    classDef decision fill:#FCE7F3,stroke:#DB2777,stroke-width:2px;
    
    class Start,End startEnd;
    class Home,Preview,HistoryPanel,ListItems process;
    class InputPrompt,SelectStyle,ClickGenerate,QRCode io;
    class LLM,SD_API,ExtractedData,BG_Image,GeneratorEngine,Assemble ai;
    class EditAction,SaveAndExport decision;
```

## 导图功能说明

1. **极简输入 (阶段 1)**：用户最大的痛点是“不想排版”，因此核心主张是 **“One Input -> One Slide”**。只需一句话和一个风格点选即可进入核心流程。
2. **黑盒生成 (阶段 2)**：大模型充当“文案策划”与“视觉导演”；SD 模型充当“绘图师”。用户在此期间只需看到优雅的 Loading 动画。
3. **微调编辑器 (阶段 3)**：不提供繁重复杂的编辑器工具（如各种属性面板）。采用所见即所得的极简模式，文字直接双击编辑，背景图通过一个按钮盲盒式再生成，排版布局一键切换。
4. **端云结合 (阶段 4)**：最终成果作为本地图片或 PDF 导出；结构化的数据在后台静默上报 BaaS 记录中心，提供历史追溯功能。
