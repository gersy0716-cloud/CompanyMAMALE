# AI 漫画数据库设计

## 连接信息

| 配置 | 值 |
|:-----|:---|
| **API 地址** | `https://data.520ai.cc/` |
| **数据库 ID** | `bsekddalnVrgIAiZYmM` |
| **表 ID (AI 漫画)** | `gaHY0ruUUs` |

---

## 表：AI 漫画 (`syOD732q5u`)

### 系统自带字段 (不需要创建)

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| `id` | Number | 自增主键 |
| `name` | Single Line Text | 漫画标题 |
| `created_by` | created_by | 创建者 |
| `updated_by` | updated_by | 更新者 |
| `created_at` | Date Time | 创建时间 |
| `updated_at` | Date Time | 更新时间 |

### 需要创建的字段

| 字段 | 类型 | 说明 | 示例 |
|:-----|:-----|:-----|:-----|
| `tenantid` | Single Line Text | **必填** 租户隔离 | `c1863285-25d1-...` |
| `prompt` | Long Text | 用户输入的故事描述 | `宇航员在火星发现一只发光的猫咪` |
| `style` | Single Select | 画风 ID | Options: `japanese_manga`, `nihonga`, `neutral`, `american_comic_90`, `american_comic_50`, `franco_belgian`, `flying_saucer`, `humanoid`, `haddock`, `armorican`, `render`, `children_book`, `medieval`, `egyptian`, `photonovel`, `stockphoto`, `random` |
| `panels_json` | Long Text | 分镜 JSON (4格) | `[{panel:1, instructions:"...", speech:"...", caption:"..."},...]` |
| `panel_images` | Long Text | 图片 URL JSON 数组 | `["https://...","https://...",...]` |
| `status` | Single Select | 生成状态 | Options: `生成中`, `已完成`, `失败` |

---

## API 操作速查

### 基础 URL

```
https://data.520ai.cc/api/bases/bsekddalnVrgIAiZYmM/tables/gaHY0ruUUs/records
```

### 创建记录

```http
POST /api/bases/bsekddalnVrgIAiZYmM/tables/gaHY0ruUUs/records
x-bm-token: {api_key}
Content-Type: application/json

{
  "name": "太空猫咪奇遇记",
  "tenantid": "c1863285-...",
  "prompt": "宇航员在火星发现一只发光的猫咪",
  "style": "japanese_manga",
  "panels_json": "[...]",
  "panel_images": "[...]",
  "status": "已完成"
}
```

### 查询记录 (带租户隔离)

```http
GET /api/bases/bsekddalnVrgIAiZYmM/tables/gaHY0ruUUs/records?filters=["where",["tenantid","=","c1863285-..."]]
x-bm-token: {api_key}
```

### 更新记录

```http
PATCH /api/bases/bsekddalnVrgIAiZYmM/tables/gaHY0ruUUs/records/{id}
x-bm-token: {api_key}
Content-Type: application/json

{ "status": "已完成", "panel_images": "[...]" }
```

### 删除记录

```http
DELETE /api/bases/bsekddalnVrgIAiZYmM/tables/gaHY0ruUUs/records/{id}
x-bm-token: {api_key}
```

---

*最后更新：2026-03-05*
