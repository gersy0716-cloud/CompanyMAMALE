# AI 漫画生成器 — 工作流程图

## 主流程图

```mermaid
flowchart TD
    Start([打开页面]) --> Init[初始化应用]
    Init --> CheckV3["自修复: 清理旧版 Token (V1/V2)\n强制升级至 V3 硬编码体系"]
    CheckV3 --> RenderPresets["渲染 17 种画风预设 (分组)"]
    Init --> RenderEngines[渲染生图引擎选择器]
    Init --> BindEvents[绑定事件监听]
    RenderPresets --> Ready[应用就绪]
    RenderEngines --> Ready
    BindEvents --> Ready

    Ready --> SelectStyle["🎨 选择漫画风格 (3 大分区)"]
    Ready --> SelectEngine["⚙️ 选择生图引擎"]
    Ready --> InputPrompt["✏️ 输入故事描述"]

    SelectStyle --> StyleGroups{17 种风格}
    StyleGroups --> GA["🌸 亚洲: 日式漫画 / 日本画"]
    StyleGroups --> GB["🦅 美式: 自然 / 现代美漫 / 复古美漫"]
    StyleGroups --> GC["🏰 欧洲: 法比漫 / 科幻 / Tintin / 3D / 克里姆特 / 中世纪 / 埃及 / 摄影 等"]
    StyleGroups --> GR["🎲 随机风格"]

    SelectEngine --> Engines{默认引擎}
    Engines --> E1["🎨 即梦 AI"]

    InputPrompt --> ClickGenerate["点击「立即生成」"]
    GA --> ClickGenerate
    GB --> ClickGenerate
    GC --> ClickGenerate
    GR --> ClickGenerate
    E1 --> ClickGenerate

    ClickGenerate --> ShowLoading["显示加载遮罩"]
    ShowLoading --> PredictPanels["📝 调用 DeepSeek 生成分镜\n(附带硬编码 Auth Params)"]
    PredictPanels --> ParseJSON["解析 4 格分镜 JSON"]
    ParseJSON --> RenderSkeleton["渲染漫画骨架卡片"]

    RenderSkeleton --> DB_Save["💾 异步保存记录至数据库\n(使用硬编码 Tenant/Token)"]
    DB_Save --> DrawPanels["🖼️ 调用所选引擎并行生图\n(附带硬编码 Auth Params)"]
    DrawPanels --> FillImages[图片加载到卡片]
    FillImages --> HideLoading[关闭加载遮罩]
    HideLoading --> Done([漫画生成完成])

    style Start fill:#e1f5e1
    style Done fill:#e1ffe1
    style ClickGenerate fill:#fff4e1
    style PredictPanels fill:#e1e5ff
    style DrawPanels fill:#f5e1ff
    style RenderSkeleton fill:#e1f5ff
    style DB_Save fill:#fff0f0
```

## DeepSeek 分镜生成详细流程

```mermaid
flowchart TD
    Start([开始生成分镜]) --> CheckRandom{随机风格?}
    CheckRandom --> |是| PickRandom[从 16 种中随机选一个]
    CheckRandom --> |否| UseSelected[使用选中的预设]
    PickRandom --> BuildPrompt
    UseSelected --> BuildPrompt

    BuildPrompt["构建 System Prompt\n漫画风格 + 4格分镜要求"] --> UserMsg["构建 User Message\n核心故事: 用户输入"]

    UserMsg --> CallAPI["POST zjAi/myUnifiedOpenAiStream\n?type=3w-api&tenant=...&author=官方&userid=...&username=...&token=...&teachertoken=..."]
    CallAPI --> CheckResp{响应格式?}

    CheckResp --> |SSE 流式| ParseSSE["解析 data: 行\n拼接 delta.content"]
    CheckResp --> |标准 JSON| ParseDirect["直接提取 choices.message.content"]
    CheckResp --> |纯文本| UseRaw[使用原始文本]

    ParseSSE --> RawContent[得到原始内容]
    ParseDirect --> RawContent
    UseRaw --> RawContent

    RawContent --> CleanJSON["清除 Markdown 代码块标记"]
    CleanJSON --> TryParse{直接解析?}

    TryParse --> |成功| Panels[4 格分镜数据]
    TryParse --> |失败| RegexMatch[正则提取 JSON 数组]
    RegexMatch --> TryParse2{再次解析?}
    TryParse2 --> |成功| Panels
    TryParse2 --> |失败| Error[解析失败错误]

    Panels --> End([返回分镜数据])
    Error --> ErrorEnd([抛出异常])

    style Start fill:#e1f5e1
    style End fill:#e1ffe1
    style ErrorEnd fill:#ffe1e1
    style CallAPI fill:#e1e5ff
```

## 图片生成流程

```mermaid
flowchart TD
    Start([开始绘制]) --> GetEngine{默认引擎}
    GetEngine --> |即梦 AI| JimengAPI["POST aiJimeng3/myTextToImage\n?type=3w-api&tenant=...&author=官方&userid=...&username=...&token=...&teachertoken=..."]

    JimengAPI --> BuildPrompts

    BuildPrompts["为每格分镜构建提示词\n风格关键词 + instructions"] --> Parallel["并行发起 4 个生图请求"]

    Parallel --> Task1["分镜 1"]
    Parallel --> Task2["分镜 2"]
    Parallel --> Task3["分镜 3"]
    Parallel --> Task4["分镜 4"]

    Task1 --> Check1{成功?}
    Task2 --> Check2{成功?}
    Task3 --> Check3{成功?}
    Task4 --> Check4{成功?}

    Check1 --> |是| Load1[加载图片]
    Check1 --> |否| Fail1["显示「绘制失败」"]
    Check2 --> |是| Load2[加载图片]
    Check2 --> |否| Fail2["显示「绘制失败」"]
    Check3 --> |是| Load3[加载图片]
    Check3 --> |否| Fail3["显示「绘制失败」"]
    Check4 --> |是| Load4[加载图片]
    Check4 --> |否| Fail4["显示「绘制失败」"]

    Load1 --> AllDone["Promise.all 完成"]
    Load2 --> AllDone
    Load3 --> AllDone
    Load4 --> AllDone
    Fail1 --> AllDone
    Fail2 --> AllDone
    Fail3 --> AllDone
    Fail4 --> AllDone

    AllDone --> End([绘制完成])

    style Start fill:#e1f5e1
    style End fill:#e1ffe1
    style Parallel fill:#e1e5ff
    style Fail1 fill:#ffe1e1
    style Fail2 fill:#ffe1e1
    style Fail3 fill:#ffe1e1
    style Fail4 fill:#ffe1e1
```

## 认证机制设计说明 (v3)

为了兼容最新的网关策略并解决本地跨域 (CORS) 拦截问题，本项目的认证机制已升级至 **V3 硬编码体系**：

- **完全硬编码认证**：所有关键鉴权参数（`token`、`teachertoken`、`userid`、`username`、`tenantId`）均已硬编码至代码逻辑中，不再依赖 URL 查询字符串或不稳定的 `localStorage` 缓存。
- **强制同步强制刷新**：通过将 `localStorage` 版本提升至 `V3`，应用会强制跳过任何旧版缓存数据，确保所有请求均使用代码中定义的最新 Hardcoded Token。
- **网关全量参数对齐**：所有发往 `mamale.vip` 的 API 请求（包括 DeepSeek 分镜生成和多引擎生图）现在均会严格附带全量查询参数：`type=3w-api`、`author=官方`、`userid`、`username` 等，确保通过网关校验。
- **数据库存储一致性**：`saveComicRecord` (520ai.cc) 与核心引擎共用同一份硬编码 TenantID 与 Token，确保数据归属的一致性。
- **自动清理机制**：`main.tsx` 中的自修复逻辑会自动识别并移除过期的 `V1` 和 `V2` 存储项，实现平滑无感升级。

## 架构图

```mermaid
flowchart LR
    subgraph Frontend["前端 (单页面)"]
        HTML["index.html\nUI 结构"]
        CSS["style.css\nInteractiveDisplay 风格"]
        JS["main.js / React (ai-comic-factory)\nV3 硬编码逻辑"]
    end

    subgraph Logic["核心模块 (V3 Hardcoded)"]
        Config["defaultSettings\n硬编码 Token/Auth 套餐\n(V3 强制同步)"]
        Presets["PRESETS\n17 种画风预设\n3 大分区"]
        Predict["predictWithMamale\n分镜生成 (附带硬编码 Params)"]
        Draw["newRender\n多引擎生图 (附带硬编码 Params)"]
        DB["saveComicRecord\n数据库保存 (硬编码 Tenant)"]
    end

    subgraph External["外部 API (mamale.vip / 520ai.cc)"]
        DS[("DeepSeek-V3\n文本生成 / 网关校验")]
        JM[("即梦 AI\n图片生成 / 网关校验")]
        Multi[("BaseMulti\n数据库记录")]
    end

    HTML --> JS
    CSS --> HTML
    JS --> Config
    JS --> Presets
    JS --> Predict
    JS --> Draw
    JS --> DB
    Predict --> DS
    Draw --> JM
    DB --> Multi

    style Frontend fill:#e1f5ff
    style Logic fill:#fff4e1
    style External fill:#f5e1ff
```

---

*最后更新：2026-03-06*
