# 码码乐踩坑记录与业务速查

> 📌 **定位**：码码乐项目开发中踩过的坑和业务速查表
>
> **何时读取**：开发码码乐相关项目时
>
> **注意**：技术栈和架构决策请查阅 `docs/PLANNING.md`

---

## 🔗 API 踩坑

### 动态子域名（必看）

> [!CAUTION]
> API 地址中的子域名**必须**使用 URL 参数中的 `type` 值！

```javascript
// ✅ 正确：动态子域名
const url = `https://${params.type}.mamale.vip/api/app/class`;

// ❌ 错误：硬编码会导致跨租户问题
const url = `https://3w-api.mamale.vip/api/app/class`;
```

---

## 🎨 AI 漫画工厂集成踩坑 (必看)

### CORS 跨域与自定义 Header (重点)

 > [!WARNING]
 > 当 API 不支持 `Access-Control-Allow-Headers` 中的自定义头（如 `__tenant`）时，预检请求 (OPTIONS) 会失败。

- **表现**：报错 `Request header field __tenant is not allowed by Access-Control-Allow-Headers in preflight response`。
- **解决**：将 `__tenant` 从 Header 移至 **URL 查询参数** 中。

 ```typescript
 // ✅ 正确：绕过 Preflight
 const res = await fetch(`${apiUrl}?__tenant=${token}`, { ... });
 ```

### 纯客户端化重构 (Next.js -> Client Only)

- **指令移除**：必须删除所有 `"use server"`，否则在 Vite/Electron 环境下无法运行。
- **环境脱耦**：移除所有 `process.env` 依赖，API Key、BaseURL、Tenant ID 全部硬编码或通过配置注入。
- **UI 简化**：移除不兼容的“设置”和“PDF 导出（依赖服务端渲染）”功能。

### 架构级瘦身：移植到纯前端单页体系

> [!TIP]
> **最佳实践**：不要强行把 Next.js 项目塞进现有轻量级项目，而是提取其核心逻辑（预设、提示词引擎）。

- 把 `ai-comic-factory` 的 **17种画风预设**（包含负向提示词处理）、API 调用组装逻辑直接抽离到 `main.js`。
- **UI 融合**：废弃 Next.js 的 React 组件，改用原生 JS 和 DOM 操作配合 Bento 风格布局。
- **认证打通**：将原先 Next.js 服务端处理的 Token 和 Tenant 参数，改为通过 API URL 参数（如 `?__tenant=`）或硬编码的 Header 解决跨域限制。

 ---

## 🗄️ BaseMulti 数据库踩坑

### 已知限制

- ❌ **filter/sort 参数不生效**：需在客户端处理
- ❌ **PATCH 被 CORS 阻止**：需安装 Allow CORS 浏览器扩展并配置白名单 `data.520ai.cc`
- ❌ **PUT 返回 404**：BaseMulti 不支持 PUT

### 字段类型

- **Number 字段必须传数字**，字符串会导致 500 错误

```javascript
// ✅ 正确
const userId = parseInt(Auth.currentUser.id);

// ❌ 错误 — "1" 而非 1
d.uploader_id = Auth.currentUser.id;
```

### 方法名

- 删除记录：`DB.remove()` 而非 `DB.delete()`
- 可用方法：`query`, `getById`, `create`, `update`, `remove`, `queryAll`, `uploadFile`

### 建表规范

- Single Select 必须手动添加所有 Options 的 Value
- 建议开启 `Value = Label`

### 发布新版本 500 错误 (重点)

> [!IMPORTANT]
> 向 `VERSIONS` 表 (`3QBYQzM3Hs`) 创建记录时：
>
> 1. **严禁**携带 `user_id` 字段！系统表不支持自定义业务映射字段，携带会导致 500 服务器错误。
> 2. **必须**提供 `name` 字段，且建议设置为版本号（如 `v1.0.2`）。

```javascript
// ✅ 正确：最小化有效载荷
await DB.create(Config.TABLES.VERSIONS, {
    name: 'v1.0.1',
    version: 'v1.0.1',
    download_url: '...',
    changelog: ''
});
```

---

## 📋 数据表 ID 速查

| 表名 | ID | 说明 |
|:---|:---|:---|
| USERS | `injhuCnRig` | 用户表 |
| TASKS | `SxqN1eraUE` | 任务表 |
| ACCOUNTS | `uVB1tBArMg` | 账号管理 |
| ANNOUNCEMENTS | `jYb1wcKsHy` | 公告 |
| SKILLS | `BCHmZn1YSZ` | Skill商店 |
| DISCUSSIONS | `gpu3NbzIg9` | 讨论 |
| GITHUB_PROJECTS | `rkZxw28qNd` | GitHub项目 |
| CLAUDES | `N6bAU62sbd` | Claude Code账号 |
| MESSAGES | `messages` | 消息 |
| VERSIONS | `3QBYQzM3Hs` | 版本更新 |
| AI_COMICS | `gaHY0ruUUs` | AI漫画生成数据 (所属库: bsekddalnVrgIAiZYmM) |

---

## 🖥️ 公司管理系统速查

### 当前版本

- `Config.CURRENT_VERSION`：`v1.0.0`（每次发版需更新）

### 权限

- **版本发布**：仅 `admin` 角色或 `admin` 用户可上传新版本。
- **消息过滤**：自己发出的消息不在自己的列表中

### 通知系统

- 仅使用 Windows 原生通知（Electron `Notification` API）
- 轮询间隔：60 秒（`NOTIFICATION_POLLING_INTERVAL`）

### Preload 桥接 API

| API | 用途 |
|:---|:---|
| `showNotification(title, body)` | 系统通知 |
| `updateBadge(count)` | 托盘角标 |
| `onNavigateTo(callback)` | 导航跳转 |
| `speakText(text)` | TTS 语音 |
| `openExternal(url)` | 打开外部链接 |
| `downloadAndInstallUpdate(url)` | 触发静默下载并安装 |
| `onUpdateProgress(callback)` | 监听下载进度更新 UI |
| `onUpdateError(callback)` | 监听下载错误 |

### Modal API

- 使用 `Modal.open(title, bodyHTML, footerHTML)` 而非 `Modal.show()`
- `footerHTML` 是字符串格式的 HTML 按钮，非数组对象

---

## 📦 v1.0.0 打包记录 (2026-02-25)

- **打包工具**：`electron-packager`（避开 `electron-builder` 的符号链接权限坑）
- **生产环境**：`devTools: false` + `Menu.setApplicationMenu(null)`
- **品牌化**：`build/create-icon.js` 生成蓝底白字 **M** 图标
- **打包参数**：`--asar --icon=build/icon.ico`
- **分发**：整个 `dist/码码乐-win32-x64` 压缩为 `.zip`
- **清理坑**：删旧目录报错时先 `taskkill /f /im "码码乐.exe" /t`

---

## 🚀 版本管理与自动更新流程

### 1. 本地打包

- 修改 `js/config.js` 中的 `CURRENT_VERSION`（如从 `v1.0.0` 改为 `v1.0.1`）。
- 修改 `package.json` 中的 `version` 字段。
- 运行 `npm run build` 生成 `码码乐 Setup 1.x.x.exe`。

### 2. 云端托管 (七牛云)

- 将生成的 `setup.exe` 手动上传到七牛云公开空间。
- 复制该文件的 **下载直链**（确保粘贴到浏览器能直接弹出下载）。

### 3. 发布版本

- 登录账号 `luowenbin`。
- 进入「版本更新」页面，点击「发布新版本」。
- 输入新版本号、更新说明和复制的**七牛云直链**。

### 4. 自动分发

- 系统每 30 分钟后台轮询一次数据库。
- 发现新版后，所有在线用户会自动开始后台静默下载。
- 下载完成后，程序静默退出、覆盖安装并自动重启，用户全程无感。

---

*最后更新：2026-02-27*
